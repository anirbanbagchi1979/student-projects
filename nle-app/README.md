# Legionary: Command – NLE Quiz App

A gamified Latin learning application for National Latin Exam preparation.

## Stack

- Vanilla HTML/CSS/JS
- Firebase (Auth + Hosting)

## Setup

Open `index.html` directly in a browser, or serve it locally with any static server.

## Environment Variables

Copy `firebase-config.js.example` to `firebase-config.js` and fill in your Firebase project credentials.
